{
    "name": "kleene",
    "private": true,
    "type": "module",

    "scripts": {
        "test": "vitest --run"
    },

    "devDependencies": {
        "vitest": "^0.30.1"
    },
    "dependencies": {
        "ret": "^0.5.0"
    }
}
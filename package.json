{
	"name": "zocker-monorepo",
	"private": true,
	"scripts": {
		"changeset": "changeset",
		"test": "pnpm test -r",
		"format": "prettier --write ."
	},
	"devDependencies": {
		"@changesets/cli": "^2.29.4",
		"prettier": "^2.8.8"
	}
}
